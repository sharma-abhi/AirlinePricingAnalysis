import java.util.*;
import org.junit.Test;
import static org.junit.Assert.assertEquals;
public class TestJunit {

   @Test
   public void testSortAvgPrice() {
      Solution sol = new Solution("Hi Word");
      HashMap<String, Double> priceHm = new HashMap<String, Double>();  
      priceHm.put("AA", 1.1);
      priceHm.put("BB", 1.3);
      priceHm.put("DD", 1.2);
      String message = "Hi Word";
      TreeMap<Double, String>  expectedResult = new TreeMap<Double, String>();  ;
      expectedResult.put(1.1, "AA");
      expectedResult.put(1.2, "DD");
      expectedResult.put(1.3, "BB");
      assertEquals(expectedResult,az.sortAvgPrice(priceHm));
   }
}
